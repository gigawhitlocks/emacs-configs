;; exists to allow a simple loading of all modules with .org files with the routine I've had in my config forever, without modification, because it's a simpel workaround and I don't want to spend more time thinking about the code that is there.
